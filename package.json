{
  "name": "quote-to-cash",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Quote-to-cash agent on Cloudflare: LLM-parsed quotes, Workflow-orchestrated approval → invoice → payment → dunning, Durable Object deal memory.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.103.0"
  }
}
